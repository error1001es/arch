# Инструкции для Gemini (и других AI)

Этот файл содержит контекст и инструкции по работе с данным репозиторием. Пожалуйста, прочитай его перед тем, как вносить изменения или отвечать на вопросы по проекту.

## 📌 Описание проекта
Этот репозиторий представляет собой **dotfiles** (конфигурационные файлы) для настройки рабочего окружения на базе **Arch Linux** с оконным менеджером **Hyprland**.

Основные компоненты:
- **Hyprland**: Оконный менеджер (Wayland compositor).
- **Waybar**: Статус-бар.
- **Kitty**: Эмулятор терминала.
- **Rofi**: Лаунчер приложений.

## 📂 Структура проекта
- `install.sh`: Скрипт первоначальной установки. Устанавливает пакеты и копирует конфиги.
- `update-configs.sh`: Скрипт для синхронизации изменений из репозитория в системную директорию пользователя (`~/.config`).
- `packages.txt`: Список пакетов для установки (pacman/AUR).
- `.config/`: Основная директория с настройками.
  - `hypr/hyprland.conf`: Главный конфиг Hyprland (бинды, правила окон, анимации).
  - `waybar/`: Конфигурация бара (`config`) и стили (`style.css`).
  - `kitty/kitty.conf`: Настройки терминала.
  - `rofi/config.rasi`: Тема и настройки меню.

## ⚙️ Рабочий процесс (Workflow)

1. **Редактирование**: Все изменения конфигурации должны вноситься в файлы внутри папки `.config/` в **корне этого репозитория**, а не напрямую в `~/.config/` пользователя (если цель — сохранить изменения в git).
2. **Применение**: После редактирования файлов, изменения применяются копированием. Для этого используется скрипт:
   ```bash
   ./update-configs.sh
   ```
3. **Зависимости**: Если для новой фичи требуется новый инструмент, проверь `packages.txt`. Если его там нет, предложи добавить его туда.

## 💡 Важные заметки для AI
- **Hyprland Syntax**: При редактировании `hyprland.conf` используй актуальный синтаксис Hyprland.
- **Waybar JSON**: Конфиг Waybar — это JSON. Будь внимателен с запятыми.
- **CSS**: Стили Waybar и Rofi используют CSS-подобный синтаксис.
- **Скрипты**: В `.config/waybar/scripts/` могут лежать кастомные скрипты. Если пишешь новый скрипт, не забудь сделать его исполняемым (`chmod +x`).

При анализе проблем или запросе на изменение настроек, сначала проверяй содержимое соответствующих файлов в `.config/`.
