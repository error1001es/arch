#!/bin/bash

# Скрипт для обновления конфигов в системе из текущей папки
# Использование: ./update-configs.sh

echo "🔄 Начинаю обновление конфигурационных файлов..."

CONFIG_DIR="$HOME/.config"
SOURCE_DIR="$(pwd)/.config"

# Проверка наличия исходной папки
if [ ! -d "$SOURCE_DIR" ]; then
    echo "❌ Ошибка: Папка .config не найдена в текущей директории."
    exit 1
fi

echo "📂 Копирую файлы из $SOURCE_DIR в $CONFIG_DIR..."

# Копируем содержимое
cp -r "$SOURCE_DIR/"* "$CONFIG_DIR/"

echo "✅ Конфиги успешно обновлены!"
echo "💡 Чтобы изменения вступили в силу, возможно, потребуется перезагрузить Hyprland или Waybar."
echo "   (Например: killall waybar && waybar &)"
